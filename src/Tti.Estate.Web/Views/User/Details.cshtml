@model UserDetailsModel
@{
    ViewData["Title"] = Localizer["Title"];
}
<form method="post">
    @Html.DisplayForModel()
    <a asp-action="Update" asp-route-id="@Model.Id" class="btn btn-secondary">
        @Localizer["Edit"]
    </a>
    <button asp-action="Delete" asp-route-id="@Model.Id" formmethod="post" class="btn btn-outline-danger">
        <i class="fa fa-trash-o"></i>
        @Localizer["Delete"]
    </button>
</form>
@section Badge {
    @{
        string badgeClass;
        switch (Model.Status)
        {
            case UserStatusModel.Active:
                badgeClass = "badge-success";
                break;
            case UserStatusModel.Blocked:
                badgeClass = "badge-warning";
                break;
            case UserStatusModel.Deleted:
                badgeClass = "badge-danger";
                break;
            default:
                badgeClass = "badge-secondary";
                break;
        }
    }
    <span class="badge @badgeClass">@Html.DisplayFor(x => x.Status)</span>
}